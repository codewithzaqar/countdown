[project]
name = "countdown"
version = "0.1.0"
description = "A count down app in Terminal."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "platformdirs>=4.3.8",
    "textual>=3.2.0",
]